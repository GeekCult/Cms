$(document).ready(

        function() {

            //$('[title]').tooltip();

            $("#info-tip-icon").tooltip({ offset: [0, 0], position: "top center", effect: 'slide'});

            
        }
    );
